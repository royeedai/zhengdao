# 代码约定

> 本文件是 AI 每次写代码时必须对照的最小一致性基准。只记录项目级约定，不重复语言或框架的默认规范。
> `/design` 阶段初始化，`/build` 实现前对照，`/verify` 检查一致性，`/postmortem` 审查是否需要更新。

## 命名约定

- **函数命名模式**：
- **变量 / 常量命名**：
- **文件 / 目录命名**：

## 代码模式

- **数据获取**：（指向示例文件，如 `src/api/example.ts`）
- **错误处理**：
- **状态管理**：

## 导入与分层

- **允许的导入方向**：
- **禁止的跨层依赖**：

## 日志约定

- **日志框架**：
- **日志级别使用**：
- **必须包含的上下文**：

## 禁止模式

- （列出本项目明确禁止的反模式）
