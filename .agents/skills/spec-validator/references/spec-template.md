# .spec 文件模板

> 复制此模板创建新的 .spec.md 文件，替换 `[占位符]` 为实际内容。

---

# [模块名] 需求规格

- **关联项目章程**：[如 `.ai-os/MISSION.md` 或项目名]
- **所属里程碑**：[M1 / M2 / Beta 等]
- **范围外内容**：[本模块明确不做什么]

## 概述

- **模块用途**：[描述此模块的功能定位]
- **目标用户**：[谁来使用这个功能，如管理员/普通用户/商户]
- **核心价值**：[为什么需要这个模块，解决什么问题]

---

## 页面/接口清单

| 编号 | 页面/接口 | 类型 | 描述 |
|------|----------|------|------|
| P-001 | [页面名] | 页面 | [简短描述] |
| A-001 | [接口名] | API | [简短描述] |

### 页面跳转关系
```
[列表页] → [新增页]
[列表页] → [编辑页]
[列表页] → [详情页]
```

---

## 功能需求

### FR-001: [功能名]

**描述**：[具体做什么]

**交互流程**：
1. 用户 [操作1]
2. 系统 [响应1]
3. 用户 [操作2]
4. 系统 [响应2]

**数据字段**：

| 字段名 | 类型 | 必填 | 校验规则 | 展示格式 | 默认态/占位符 | 说明 |
|--------|------|------|---------|---------|-------------|------|
| name | string | 是 | 1-100字符 | 直接显示 | - | 商品名称 |
| category_id | uint | 是 | 必须存在 | 显示分类名称 | “请选择分类” | 所属分类 |
| price | decimal | 是 | >0 | ¥XX.XX | - | 单价 |
| image | string | 否 | jpg/png,<2MB | 缩略图 | - | 商品图片 |
| status | int | 是 | 0或1 | 标签(上架/下架) | “全部”(筛选时) | 上架状态 |
| created_at | datetime | 自动 | - | YYYY-MM-DD HH:mm | - | 创建时间 |

**状态处理**：
- 加载中：[显示什么]
- 空数据：[显示什么]
- 错误：[显示什么]
- 成功：[显示什么]

**权限要求**：[谁能看/谁能操作]

**边界条件**：
- [条件1]：[怎么处理]
- [条件2]：[怎么处理]

---

## 数据模型

### [表名] 表

| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | 自增主键 | PK | - | 主键 |
| name | 字符串(255) | NOT NULL | - | 名称 |
| status | 小整数 | NOT NULL | 1 | 状态 |
| created_at | 时间戳 | NOT NULL | 当前时间 | 创建时间 |
| updated_at | 时间戳 | NOT NULL | 当前时间 | 更新时间 |
| deleted_at | 时间戳 | NULL | NULL | 软删除时间 |

### 索引

| 索引名 | 字段 | 类型 | 说明 |
|--------|------|------|------|
| idx_status | status | 普通 | 状态筛选 |
| uk_code | code | 唯一 | 编码唯一 |

### 关联关系
- [表A] 1 : N [表B]（通过 `xxx_id` 关联）

---

## API 定义

### A-001: [接口名]

- **URL**: `[METHOD] /api/v1/[resource]`
- **权限**: [需要登录/需要管理员权限]

**请求参数**：

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| page | query | int | 否 | 页码，默认1 |
| page_size | query | int | 否 | 每页条数，默认20 |
| keyword | query | string | 否 | 搜索关键字 |

**成功响应**：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [...],
    "total": 100
  }
}
```

**错误响应**：

| 错误码 | HTTP状态 | 说明 |
|--------|---------|------|
| 40001 | 400 | [参数错误说明] |
| 40400 | 404 | [资源不存在] |

---

## 非功能需求

- **性能**：[如列表接口响应时间 < 500ms]
- **安全**：[如敏感数据加密存储]
- **适配范围 / 支持环境**：[如支持 Web / iOS / Android / 浏览器范围 / 运行环境]

---

## 关联模块

| 模块 | 关系 | 说明 |
|------|------|------|
| [模块A] | 依赖 | [如何依赖] |
| [模块B] | 被依赖 | [如何被依赖] |

---

## 验收标准

### 正常流程
- [ ] **AC-001**：Given [前置条件], When [用户操作], Then [预期结果]
- [ ] **AC-002**：...

### 异常流程
- [ ] **AC-E001**：Given [异常条件], When [用户操作], Then [预期错误处理]
- [ ] **AC-E002**：...

---

## 交付证据（推荐）

- **关键用户任务 / 运营任务验证**：[如完整闭环录屏、步骤日志、UAT、关键命令输出]
- **构建/测试证据**：[如 `npm test` / `go test` / `cargo test` 的结果]
- **关键接口证据**：[如请求/响应样例]
- **关键界面证据**：[如截图、录屏、链接]
- **迁移/发布注意事项**：[如是否涉及数据库迁移、Feature Flag、回滚]
