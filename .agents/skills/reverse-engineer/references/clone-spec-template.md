# 参考驱动项目 .spec 文件模板

> 本模板用于复刻/仿制项目。在标准 spec 基础上增加了参考来源、置信度标记和差异声明。
> 每条内容后的 `[确认]` / `[推断]` / `[待确认]` 标记表示信息来源的可靠程度。
> 默认以当前项目为主体描述；除非用户明确要求迁移或兼容，不要在正文中出现 `legacy`、"原项目"、"兼容旧系统" 等表述。

---

# [模块名] 需求规格（参考驱动）

## 0. 项目关联

- **关联项目章程**：[如 `.ai-os/MISSION.md`]
- **所属里程碑**：[M1 / M2 等]
- **范围外内容**：[本模块明确不做什么]

## 参考来源

- **参考对象**：[名称或代号，仅用于识别素材来源]
- **参考方式**：功能逻辑参考 / UI 风格参考
- **素材清单**：

| 素材类型 | 文件/路径 | 说明 |
|---------|----------|------|
| 截图 | `.ai-os/references/clone-materials/screenshots/[文件名]` | [描述] |
| URL | [参考 URL] | [可访问状态] |
| API 文档 | `.ai-os/references/clone-materials/api/[文件名]` | [描述] |
| 参考源码 | `.ai-os/references/clone-materials/code-reference/[路径或说明]` | [仓库 / commit / 关键代码入口] |
| 文字描述 | [来源] | [描述] |

- **本项目明确差异**：
  - [差异 1：如"不做 XX 功能"]
  - [差异 2：如"支付模块替换为 XX"]
- **命名约束**：
  - [当前项目默认不沿用参考对象品牌名/模块名]
  - [如需保留映射，仅记录在此，不写入正文命名]

## 参考锚点

> 本节只记录实现阶段真正需要回查的旧代码入口，避免把整套参考仓库当成当前项目边界。

- **参考代码地图**：`.ai-os/codebase-map.md`
- **模块对应参考路径**：[仓库路径 / 目录 / 文件]
- **关键逻辑入口**：
  - [函数 / 类 / store / job / SQL / handler]
- **必须保持一致的行为**：
  - [行为 1]
- **允许改写的行为**：
  - [行为 1]
- **实现前待确认**：
  - [待确认点]

---

## 1. 概述

- **模块用途**：[描述] `[确认/推断]`
- **目标用户**：[谁来使用] `[确认/推断]`
- **核心价值**：[为什么需要] `[确认/推断]`

---

## 2. 页面/接口清单

| 编号 | 页面/接口 | 类型 | 描述 | 置信度 |
|------|----------|------|------|--------|
| P-001 | [页面名] | 页面 | [简短描述] | `[确认/推断]` |
| A-001 | [接口名] | API | [简短描述] | `[确认/推断]` |

### 页面跳转关系
```
[列表页] → [新增页]
[列表页] → [编辑页]
[列表页] → [详情页]
```

---

## 3. 功能需求

### FR-001: [功能名] `[确认/推断]`

**描述**：[具体做什么]

**交互流程**：
1. 用户 [操作1]
2. 系统 [响应1]
3. 用户 [操作2]
4. 系统 [响应2]

**数据字段**：

| 字段名 | 类型 | 必填 | 校验规则 | 展示格式 | 默认态/占位符 | 置信度 | 说明 |
|--------|------|------|---------|---------|-------------|--------|------|
| name | string | 是 | 1-100字符 | 直接显示 | - | `[确认]` | 名称 |

**状态处理**：
- 加载中：[显示什么] `[推断]`
- 空数据：[显示什么] `[推断]`
- 错误：[显示什么] `[推断]`
- 成功：[显示什么] `[推断]`

**权限要求**：[谁能看/谁能操作] `[确认/推断/待确认]`

**边界条件**：
- [条件1]：[怎么处理] `[推断/待确认]`

---

## 4. 数据模型

> 以下数据模型从 API 入参返参反推，标记为 `[推断]` 的字段需确认。

### [表名] 表

| 字段 | 类型 | 约束 | 默认值 | 置信度 | 说明 |
|------|------|------|--------|--------|------|
| id | 自增主键 | PK | - | `[推断]` | 主键 |

### 索引

| 索引名 | 字段 | 类型 | 置信度 | 说明 |
|--------|------|------|--------|------|
| idx_status | status | 普通 | `[推断]` | 状态筛选 |

### 关联关系
- [表A] 1 : N [表B]（通过 `xxx_id` 关联）`[推断]`

---

## 5. API 定义

### A-001: [接口名]

- **URL**: `[METHOD] /api/v1/[resource]` `[确认/推断]`
- **权限**: [需要登录/需要管理员权限] `[推断]`

**请求参数**：

| 参数名 | 位置 | 类型 | 必填 | 置信度 | 说明 |
|--------|------|------|------|--------|------|
| page | query | int | 否 | `[确认]` | 页码 |

**成功响应**：
```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

**错误响应**：

| 错误码 | HTTP状态 | 置信度 | 说明 |
|--------|---------|--------|------|
| 40001 | 400 | `[推断]` | [说明] |

---

## 6. 非功能需求

- **性能**：[要求] `[待确认]`
- **安全**：[要求] `[待确认]`
- **适配范围 / 支持环境**：[如 Web / iOS / Android / 浏览器范围 / 运行环境] `[待确认]`

---

## 7. 关联模块

| 模块 | 关系 | 置信度 | 说明 |
|------|------|--------|------|
| [模块A] | 依赖 | `[推断]` | [如何依赖] |

---

## 8. 验收标准

### 正常流程
- [ ] **AC-001**：Given [前置条件], When [用户操作], Then [预期结果] `[确认/推断]`

### 异常流程
- [ ] **AC-E001**：Given [异常条件], When [用户操作], Then [预期错误处理] `[推断]`

---

## 待确认事项汇总

> 以下条目信息不足，需要用户或对方补充确认后才能进入开发。

| 编号 | 相关 FR/AC | 待确认内容 | 当前假设 |
|------|-----------|-----------|---------|
| Q-001 | FR-001 | [待确认的具体问题] | [当前假设的处理方式] |

---

## 9. 交付证据（推荐）

- **构建/测试证据**：[如 `npm test` / `go test` 的结果]
- **关键接口证据**：[请求/响应样例]
- **关键界面证据**：[截图、录屏]
- **与参考素材对照**：[功能逻辑或 UI 参考一致性说明]
