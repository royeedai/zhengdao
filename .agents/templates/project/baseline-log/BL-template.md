# {{INITIAL_BASELINE_ID}}

> 共享基线记录：每条记录单独成文件，供多人协作时对齐和审计使用。它不是新的需求真理源，需求定义仍以 `MISSION.md + specs/` 为准。
> 新增记录时新建 `CR-YYYYMMDD-HHMMSS-change-request.md` 或 `BL-YYYYMMDD-HHMMSS-scope-update.md` 这类文件，不回写或覆盖历史记录。会改变 `MISSION.md` 的基线更新应先通过独立 baseline-sync 提交落到主干，再进入并行实现。

- **Type**: align
- **Status**: confirmed
- **Summary**: 初始交付基线已确认
- **Affects**: MISSION.md, specs/example.spec.md
- **Confirmed At**: {{INITIAL_BASELINE_DATE}}
