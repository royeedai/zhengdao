# [宿主项目名 / 当前交付主题] Mission

> 本文件与 `.ai-os/specs/` 一起构成当前交付的唯一需求真理源。`MISSION.md` 只记录低频、已确认、共享的交付基线；待确认项、阶段状态和协作过程记录不要写在这里。
> 任何需求补充、范围调整、验收变化都必须先走 `/change-request`，先在 `baseline-log/` 中新增影响分析与确认记录，再按需更新本文件和相关 spec，最后才进入代码改动。
> `当前基线 ID` 对应当前 confirmed baseline record 的文件名（不含 `.md`），默认使用 `BL-YYYYMMDD-HHMMSS-slug` 命名。
> `greenfield` / `reverse-spec` 下，本文件通常接近整个项目目标；`brownfield` / `change` 下，本文件记录的是“当前这轮交付基线”，不是整个存量项目本身，只保留理解本轮交付所需的宿主项目上下文。
> `当前交付目标` 必须写成“交付给谁、形成什么结果”的结果描述，不能写成“先做 /design /plan /build”这类流程动作。

## 1. 交付基线摘要

- **宿主项目 / 系统**：
- **当前交付主题**：
- **当前交付目标**：[写清交付给谁、形成什么结果；不要写“先锁设计再开发”]
- **成功标准**：
- **项目模式**：greenfield / reverse-spec / brownfield / change
- **当前交付档位**：exploratory / standard / high-risk
- **当前治理档位**：P0 / P1 / P2
- **当前基线 ID**：{{INITIAL_BASELINE_ID}}

## 2. 用户与闭环场景

- **目标用户**：
- **关键场景**：
- **目标市场 / 使用环境**：
- **当前最小可行闭环**：
- **明确后续迭代项**：

## 3. 已确认约束与关键决策

- **已确认技术栈与关键选型**：
- **已确认目标运行态 / 部署约束**：
- **已确认质量优先级**：逻辑正确 > 关键体验 > 工程可维护 > 交付速度
- **必须保持的宿主项目约束**：
- **已确认核心设计决策**：
- **已确认核心逻辑决策**：

### 已确认非功能性约束
- [性能 / 安全 / 可用性 / 合规 / 可维护性等已确认约束]

## 4. 范围边界与非目标

> 本节用于锁定“做什么 / 不做什么”。任何超出范围的工作都必须先走 `/change-request`。
> `brownfield` / `change` 下，这里的范围内 / 范围外只描述本轮交付，不等于整个宿主项目。

### 范围内
- [必须交付的内容]

### 范围外
- [明确不做的内容]

### 非目标
- [本轮明确不处理的优化、重构、体验增强]

### 核心需求清单
| 需求 ID | 需求点 | 来源 | 验收入口 |
|---------|--------|------|----------|
| REQ-001 | [需求描述] | [来源] | [AC-001] |

## 5. 稳定风险与外部依赖

- **外部依赖**：
- **稳定风险**：
- **高风险触发因素**：
- **影响范围摘要**：
- **审批点**：
